@model PharmaSmartWeb.Models.Suppliers

@{
    ViewData["Title"] = $"تعديل المورد: {Model.SupplierName}";
    Layout = "_ModalLayout";
    ViewBag.ModalPanelSize = "lg";
    ViewBag.ModalHeaderIcon = "local_shipping";
}

<!-- لا نحتاج لحاوية enterprise-form-panel هنا لأن الـ ModalLayout يقوم بدور الحاوية، لكن سنستخدم عناصر النموذج الموحدة -->
<form asp-action="Edit" method="post">
    @Html.AntiForgeryToken()
    
    @if (ViewBag.Error != null || TempData["Error"] != null)
    {
        <div class="enterprise-alert-error">
            <span class="material-symbols-outlined" style="font-size: 16px; vertical-align: text-bottom;">warning</span>
            @(ViewBag.Error ?? TempData["Error"])
        </div>
    }

    <input type="hidden" asp-for="SupplierId" />
    <input type="hidden" asp-for="AccountId" />
    <input type="hidden" asp-for="CreatedAt" />

    <!-- عرض الحساب المالي (للقراءة فقط) باستخدام Enterprise Info Box -->
    <div class="enterprise-info-box" style="align-items: center;">
        <span class="material-symbols-outlined" style="font-size:24px; color:#475569;">account_balance</span>
        <div style="flex:1; min-width:0;">
            <div style="font-size:11px; color:#64748b; font-weight:700; margin-bottom: 2px;">الحساب المالي المرتبط بالدليل</div>
            <div style="font-size:13px; font-weight:700; color:#0f172a; font-family: monospace;">
                @(Model.Account?.AccountCode ?? "---") — @(Model.Account?.AccountName ?? "غير مربوط")
            </div>
        </div>
        <span style="font-size:11px; background:#e2e8f0; padding:4px 8px; border-radius:2px; color:#475569; font-weight:700;">للقراءة فقط</span>
    </div>

    <div class="enterprise-section-title">بيانات المورد الأساسية والتواصل</div>
    
    <div class="enterprise-row">
        <div>
            <label class="enterprise-label">اسم المورد <span style="color:#e11d48;">*</span></label>
            <input asp-for="SupplierName" required class="enterprise-input" placeholder="مثال: شركة الأدوية العالمية" />
            <span asp-validation-for="SupplierName" style="color:#e11d48; font-size:11px; margin-top:4px; display:block;"></span>
        </div>
        <div>
            <label class="enterprise-label">المسؤول عن التواصل</label>
            <input asp-for="ContactPerson" class="enterprise-input" placeholder="الاسم الثلاثي للمندوب" />
        </div>
        <div>
            <label class="enterprise-label">رقم الهاتف / الجوال</label>
            <input asp-for="Phone" class="enterprise-input" dir="ltr" placeholder="05XXXXXXXX" type="tel" />
        </div>
        <div>
            <label class="enterprise-label">العنوان بالتفصيل</label>
            <input asp-for="Address" class="enterprise-input" placeholder="المدينة، الحي، اسم الشارع، رقم المبنى" />
        </div>
    </div>

    <div class="enterprise-section-title" style="margin-top: 24px;">حالة النظام</div>

    <!-- التحكم بالحالة بأسلوب متناسق -->
    <div style="border: 1px solid #e2e8f0; border-radius: 2px; padding: 12px 16px; display: flex; justify-content: space-between; align-items: center; background: #fff;">
        <div style="display:flex; align-items:center; gap:12px;">
            <span class="material-symbols-outlined" style="font-size:20px; color:#063955;">verified_user</span>
            <div>
                <div style="font-size: 13px; font-weight: 700; color: #0f172a;">حالة المورد (النشاط)</div>
                <div style="font-size: 11px; color: #64748b; margin-top: 2px;">تفعيل أو إيقاف التعامل مع المورد (الإيقاف لن يؤثر على الفواتير السابقة)</div>
            </div>
        </div>
        <label style="display:flex; align-items:center; gap:8px; cursor:pointer; background: #f8fafc; padding: 6px 12px; border: 1px solid #cbd5e1; border-radius: 2px;">
            <input type="checkbox" name="IsActive" value="true" @(Model.IsActive == true ? "checked" : "") style="width:16px; height:16px; cursor:pointer; accent-color:#063955;" />
            <input type="hidden" name="IsActive" value="false" />
            <span style="font-size:12px; font-weight:700; color:#063955;">@(Model.IsActive == true ? "حساب نشط" : "حساب موقوف")</span>
        </label>
    </div>

    <div class="enterprise-actions">
        <button type="button" onclick="modalNavigateBack()" class="enterprise-btn-cancel">إلغاء التعديل</button>
        <button type="submit" class="enterprise-btn-warning">
            <span class="material-symbols-outlined" style="font-size:16px;">save</span>
            حفظ التحديثات
        </button>
    </div>
</form>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}